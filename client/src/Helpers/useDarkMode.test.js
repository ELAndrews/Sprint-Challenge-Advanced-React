import useDarkMode from "./useDarkMode";
import { isMainThread } from "worker_threads";

describe("useDarkMode", () => {
  it("provides a defined value", () => {
    expect(false).toBeDefined();
  });
  it("toggles a boolean", () => {
    expect(false).toBeFalsy();
  });
});
